COMPLETENESS CONTRACT: your JSON must enumerate EVERY individually visible face, edge, opening, attachment and downspout across ALL photos - a partial, sampled or summarized list is a WRONG answer. Count the openings you can see across all photos FIRST (during your reasoning), then emit exactly that many entries. Raw estimates must come from genuinely independent chains - never duplicate the same value twice. Take the time you need; do not stop early.

Analyze the attached facade photos fully autonomously, no follow-up questions. All measurements in mm, unrounded. You EXTRACT; all statistics are computed downstream in code - do not aggregate, average, or self-assess spread.

0. COUNTRY AND REFERENCE TABLE:
Determine the most likely country from visible cues (license plates, brick format and bond, window style, roller shutters, mailbox type, outlets, signage language) and state the determination plus certainty in meta.notes.
__REFERENCE_CATALOG__
meta.country is a CALCULATION PROFILE, not the detected country: set "US" if the detected country uses imperial construction conventions, otherwise "DE". If the detected country is neither US nor DE, add to quality.warnings: "Detected country <X> outside supported calc profiles; measurements use <X> reference dimensions, calc profile <US|DE> assigned." 

1. FIND REFERENCES (per photo): list EVERY object with a standardized or statistically known size as an entry in the references array: photo_index, object, assumed_size_mm from the country table, reliability (standardized = high, market-typical = medium, statistical = low), plane, and bounding_box [x_min, y_min, x_max, y_max] normalized 0-1 in that photo. bounding_box is MANDATORY - if you cannot localize an object well enough to box it, it is not a reference.
2. VIEW ANGLE (per photo): report view_angle in meta.photos. For WIDTH measurements you MUST correct foreshortening: estimate the facade yaw from vanishing geometry (convergence of eave and plinth lines, window-row spacing) and divide the apparent width by cos(yaw); set perspective_corrected=true for that photo when you applied such a correction and state the estimated yaw in meta.notes. Heights need no yaw correction. Do not claim any other rectification. Confidence of any photo-derived value is capped by that photo's angle: frontal -> up to high, slightly_angled -> up to medium, steeply_angled -> low.
3. RAW ESTIMATES: for each target quantity (total_width, total_depth, eave_height, ridge_height) give one independent estimate per usable reference per photo as entries in quality.raw_estimates: target, photo_index, reference_object, assumed_size_mm, estimate_mm, reliability, via_transfer. Emit every raw estimate; do not discard or average anything. A reference from another photo may only be used via a shared element visible in both photos; mark such estimates via_transfer = true. For total_width, produce raw estimates from at least TWO DIFFERENT reference types (e.g. entry door AND brick coursing AND vehicle/license plate) whenever more than one is boxable; a single-reference-type width is acceptable only when nothing else exists - then add a quality.warnings entry naming the missing diversity.
4. ELEMENTS: measure the FULL DELIVERABLE below using the per-photo references. For footprint width, eave height and ridge height enter your single best value with confidence "low" and low_reason "pending deterministic consolidation" - final values and confidence are computed downstream from raw_estimates. Opening areas/perimeters and wall net areas are also recomputed downstream; still fill your best values.

FULL DELIVERABLE - a complete run covers ALL of these; a category that is genuinely not assessable from the inputs gets nulls plus one quality.warnings entry, never a silent omission:
- Building: building_type, stories, roof_type, shared_walls (duplex/townhouse).
- Footprint: width_mm, depth_mm, perimeter_mm, area_mm2 (and the points polygon where derivable).
- Heights: eave_height_mm, ridge_height_mm; parapet_height_mm only for parapet/flat roofs. On sloped sites or where eave/ridge differ per side, ALSO fill heights.per_elevation (eave/ridge/grade_offset per elevation) and heights.datum_note stating where grade zero was taken.
- Faces: every roof facet (RF-n) with sloped area_mm2 and pitch (degrees_original + degrees_rounded; rise_over_12_snapped only for US calc profile); every wall (WL-n) per elevation with gross area_mm2 and net_area_mm2; soffit (SF-n) and fascia (FC-n) with soffit_depth_mm where assessable; material where visible.
- Edges: every roofline and outline edge (E-n) with edge_class, length_mm and belongs_to_elevation. Report ridge, hip, valley, eave and rake edges individually - their per-class totals are the deliverable a roofer buys.
- Openings: every window, door, patio_door, garage_door and skylight (W/D/G/SK-n) with width_mm, height_mm, sill_height_mm, position_mm, area_mm2, perimeter_mm.
- Attachments: every dormer, bay, balcony, awning, addition and chimney with width/height/depth where assessable, PLUS placement so it can be modeled: position_mm (lower-left corner on the parent elevation, like openings; for roof objects x along the eave from its left end, y from the eave upward along the slope), parent_face_id (the WL-n/RF-n it sits on), attached (false for free-standing garages/outbuildings - never merge those into the house), include_in_footprint, and for dormers dormer.style plus dormer.face_pitch_deg. Pipes and vents count as attachments too (see PENETRATION RULE).
- Downspouts: every visible drop as DS-n (see DOWNSPOUT RULE).
- Condition areas: photo-visible damage as CA-n (see CONDITION RULE).
Edge lengths, roof areas and heights are cross-checked downstream against footprint + pitch geometry. Keep them mutually consistent (e.g. a gable ridge equals the footprint side it runs along) instead of estimating each number in isolation.

MEASUREMENT DEFINITIONS (use exactly these):
- Eave height: ground line to the underside of the eave/soffit where it meets the wall, excluding the gutter.
- Ridge height: ground line to the topmost point of the roof covering at the ridge.
- Total width / depth: outer face of finished wall to outer face of finished wall, excluding roof overhangs.
- Footprint scope (building.footprint): the MAIN building body only - the principal volume, EXCLUDING attached annexes, garages, porches and bays; report those as attachments. ALSO fill building.footprint_overall with the envelope width/depth INCLUDING attached secondary volumes (excluding detached structures). Always fill both; they are equal when nothing is attached. Never silently mix the two scopes.
- Window size: visible outer frame dimensions as seen on the facade (not rough opening, not glass).
- Door size: visible outer frame dimensions including the frame, excluding decorative surrounds.
- Opening position: x/y of the lower-left corner of the opening, measured from the lower-left corner of its elevation as seen standing outside facing that elevation; x increases to the right in that view.
- Face area: area_mm2 is gross including openings; net_area_mm2 is gross minus all openings.
- Roof face area: the SLOPED (true) surface area, never the horizontal plan projection.
- Edge classes: ridge = horizontal top edge; hip = sloped external junction of two roof faces; valley = sloped internal junction; eave = horizontal lower roof edge; rake = sloped gable-end roof edge; outside_corner / inside_corner = vertical wall corners; head / sill / jamb = top / bottom / side edge of an opening (report only when individually relevant). Use "unclassified" instead of guessing.
- Soffit: the horizontal underside of the roof overhang; soffit_depth_mm = horizontal distance from wall face to fascia.
- Fascia: the vertical board capping the eave end of the overhang; its face area = fascia run length x fascia height.
- Footprint perimeter: total outline length including every jog and bay; footprint area is enclosed by the same outline.

BRICK FORMAT RULE: before using brick coursing as a reference, determine the format from the stone proportions (stretcher length : height): DE Normalformat NF 240x71 mm -> course incl. joint 83.3 mm; Duennformat DF 240x52 -> 62.5 mm; Reichsformat (pre-1950) 250x65 -> ~78 mm; US modular 194x57 -> 67.7 mm. State the detected format in the reference entry. If the facade may be brick slips on insulation (Riemchen: very flat reveals, unusually perfect joints, modern windows in an 'old' wall), down-weight coursing to reliability low and add a warning.

REFERENCE DISAGREEMENT RULE: outlier discarding applies to single estimates, never to whole reference TYPES. If two reference types (e.g. door-based vs brick-based vs vehicle-based) disagree systematically by more than 8%, do NOT drop the minority type: keep both chains in raw_estimates, cap the affected anchor's confidence at low, and add one quality.warnings entry naming both values and types so the conflict stays visible downstream.

WIDTH MEASUREMENT RULES: (a) total_width raw estimates MUST include at least one HORIZONTAL reference chain - brick stretcher coursing counted ACROSS the facade, a window-sum chain (sum of measured window widths plus the piers between them), a vehicle length, or a garage door width; append '(horizontal)' to the reference_object of every such chain. A width derived only from vertical references (door height etc.) gets reliability low plus one quality.warnings entry naming the missing horizontal chain. (b) Apply the yaw correction from the view-angle step to EVERY width estimate taken from a non-frontal photo - an uncorrected apparent width from an angled photo is systematically too small. (c) If a facade corner lies in the outer 10% of the image frame, wide-angle lens distortion may compress it: add a meta.notes entry and prefer photos with margin around the facade when both exist.

VERTICAL ANCHOR RULE: eave/ridge/sill heights must be scaled from at least one VERTICAL reference chain (door height, storey height, vertically counted brick courses) - never purely from horizontal references. Produce raw estimates for eave_height from at least TWO different vertical chains when available. If the ground line is occluded (fence, vehicles, vegetation), state the assumed ground offset in low_reason, add one quality.warnings entry, and cap confidence at medium. Photos looking steeply upward compress verticals: cap height confidence at low and prefer chains from the most level photo available.

DEPTH BASIS RULE: if the TRIAGE FACTS (or the stage 1 result) report depth_basis "oblique_only", cap footprint depth confidence at low and add one quality.warnings entry naming the missing frontal side view.

OCCLUSION RULE: for any elevation whose photos are more than 30% occluded (see per-file occluded_percent in the TRIAGE FACTS), add one summary warning "openings likely undercounted on <elevation>" instead of silently omitting hidden openings.

DEPTH ESTIMATES RULE: footprint depth must be backed by raw estimates exactly like the width: emit at least TWO independent quality.raw_estimates entries with target 'total_depth' whenever any photo shows the side/depth elevation frontally or obliquely (use references on the side facade, yaw-rectified horizontal chains, or roof geometry via ridge/eave offsets as independent paths). Only when no photo shows any part of the side may depth remain a single estimate; then the DEPTH BASIS RULE applies (confidence low + warning). Never base depth on a single transferred reference when a second independent path exists.

EAVE LINE AND DATUM RULE: eave_height always means the MAIN roof eave line of the reference facade (normally the front/street elevation). On cross-gable houses or houses with annexes and multiple eave lines, never mix chains to different eave lines into one target: measure secondary/annex/wing eave lines separately and report them ONLY in heights.per_elevation, never as eave_height raw estimates. Filling heights.per_elevation is MANDATORY for every photographed elevation (eave_mm, ridge_mm where visible, grade_offset_mm). Grade zero (datum) is the ground line at the REFERENCE facade - not the lowest visible point of the site; name the datum explicitly in heights.datum_note and express other elevations via grade_offset_mm relative to it.

USER REFERENCE RULE: a user-provided reference measurement overrides all photo-side assumptions (reference object sizes). It does NOT override a legible written plan dimension; if they disagree by 3% or more, keep the plan value and add a quality.warnings entry describing the disagreement. If a USER REFERENCE is provided, it is the PRIMARY scale: the reference object is described in free text - locate it yourself in the photo(s) where it is visible, anchor every chain in those photos on it, and transfer it to other photos via shared elements where possible (via_transfer=true). Table assumptions (door heights etc.) then serve only as cross-checks: report the door height IMPLIED by the user reference; if it deviates >5% from the table assumption, add a quality.warnings entry naming both values. Chains anchored on the USER REFERENCE must set reference_object to 'user_provided: <object>'.

ROOF COVERING RULE (v1.5): for every roof face (RF-n) set material to the covering type: shingle_asphalt / shingle_wood / metal / tile / slate / membrane. Judge from texture and geometry (shingle tabs, standing seams, tile courses, membrane seams), NEVER from color alone. If the covering is not identifiable, set material to "unknown" - schema validation rejects wall substrates on roof faces and vice versa. Wall faces (WL-n) take wall substrates only (stucco / brick / siding / wood / concrete / eifs; metal is valid for both classes).

FACE COLOR RULE (v1.4): for every face, fill color with the dominant surface color: hex (sRGB), a short human name, and confidence. Add secondary_hex for a clear second tone on the same face (e.g. brick mortar). Shadow, white balance and color cast shift hues - reflect that in confidence and note it in color.note (e.g. "face in shadow in all photos"). This drives 3D rendering only; it is never a paint-order color.

PENETRATION RULE (v1.4): pipes (plumbing stacks) and vents (roof/wall vents incl. exhaust caps) are attachments with type "pipe" / "vent", each with position_mm and parent_face_id. They fall under the COMPLETENESS CONTRACT: count them across all photos first, then emit exactly that many entries - pipe boots and vents are per-piece quote positions, an undercount is a wrong answer.

DOWNSPOUT RULE (v1.4): report every visible downspout drop in the top-level downspouts array (DS-n): elevation, length_mm (approximately the eave height of that elevation when not directly measurable, with source "estimated"), position_mm.x along its elevation, and connects_edge_id (the eave edge E-n whose gutter it serves) where identifiable. Gutter RUNS stay derived downstream from eave edges - only the drops are reported here.

CONDITION RULE (v1.4): report ONLY damage that is actually visible in the photos in condition_areas (CA-n): type (failing_paint / rot / sheathing_damage / decking_damage / mold_mildew / rust / crack / missing_material), parent_face_id, elevation, area_mm2, severity (light / moderate / severe), position_mm, and photo_index of the photo where it shows best. Never speculate about hidden damage; a shadowed or partially visible finding gets confidence low with a low_reason. No visible damage means an empty array - not a warning.

OUTPUT: fill exactly the attached JSON schema. Emit the top-level keys in exactly this order: meta, building, references, attachments, faces, edges, openings, downspouts, condition_areas, quality - most critical first, so an unexpected cutoff loses only the tail. Return ONLY the JSON object - no markdown fences, no commentary before or after. Per value: reference_used, confidence, source. Not visible or uncertain: null plus a reason. Do not invent elements or reference objects. Do not pad the JSON with placeholder entries.
